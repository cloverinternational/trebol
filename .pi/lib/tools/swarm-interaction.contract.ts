/** Model-facing contracts for the tools registered by this module. */
import type { ToolContract } from "../runtime/tool-contract.ts";

export const ASK_USER_QUESTION_CONTRACT: ToolContract = {
  description: "Ask the user a question and wait for their response. Use this when clarification\nwould materially change the outcome — don't use it for questions you can answer from context\nor for simple guesses you can verify with a quick test.\n\nFor several related decisions, send one questionnaire with 'questions' instead of\n'question'. Each item requires a unique id, a prompt, and structured options:\n\n  {\"questions\":[{\n    \"id\":\"database\",\n    \"prompt\":\"Which database should we use?\",\n    \"label\":\"Database\",\n    \"options\":[\n      {\"value\":\"postgres\",\"label\":\"PostgreSQL\",\"description\":\"Relational and full-featured\"},\n      {\"value\":\"sqlite\",\"label\":\"SQLite\",\"description\":\"Simple embedded storage\"}\n    ],\n    \"allow_other\":false\n  }]}\n\nOmitting allow_other permits a custom answer. Set it to false to require one of the\nlisted values. Do not combine 'question' and 'questions'.\n\n## When to prefer 'visual_choice' over 'choice'\n\nStrongly prefer 'visual_choice' any time the answer is easier to SEE than to read:\n\n- UI / layout / component design (\"which layout?\", \"which button style?\", \"which nav?\")\n- Architectural options that map to diagrams (\"which service topology?\", \"which state machine?\")\n- Side-by-side comparisons where prose would be dense (\"flat vs tree structure?\")\n- Any \"show me options\" where you would otherwise describe three designs in a paragraph\n\nIf the user says \"plan / design / layout / mock up / sketch / wireframe / compare\", treat\nthat as a strong signal to enter plan mode AND to offer 'visual_choice' for decisions that\nbenefit from visual comparison. Do NOT fall back to a bulleted list in chat when a\nvisual_choice picker would be a better fit.\n\n## visual_choice payload\n\nSupply a 'visual' object with a 'kind' discriminator and primitive-specific fields:\n\n  { \"kind\": \"options\",  \"items\": [{\"key\",\"label\",\"description?\"}, ...] }\n  { \"kind\": \"cards\",    \"items\": [{\"key\",\"title\",\"description?\",\"body?\"}, ...] }\n  { \"kind\": \"split\",    \"items\": [ {A}, {B} ] }   // exactly 2 items\n  { \"kind\": \"markdown\", \"content\": \"# prose + fenced mermaid blocks\" }\n  { \"kind\": \"mermaid\",  \"diagram\": \"flowchart TD\\n...\", \"caption?\": \"...\" }\n  { \"kind\": \"raw_html\", \"content\": \"<div>...</div>\" }\n\nInteractive kinds (options/cards/split) accept any presentation kind as a nested 'body':\n\n  {\"kind\":\"cards\",\"items\":[\n    {\"key\":\"flat\",\"title\":\"Flat\",\"body\":{\"kind\":\"mermaid\",\"diagram\":\"graph TD\\nA-->B\\nA-->C\"}},\n    {\"key\":\"tree\",\"title\":\"Tree\",\"body\":{\"kind\":\"mermaid\",\"diagram\":\"graph TD\\nR-->A-->C\"}}\n  ]}\n\nThe active client renders the picker inline; in the TUI the user reviews and selects it in the terminal.\n\n## Other types\n\n- 'text' — free-form input\n- 'choice' — single text option (only when visual treatment adds nothing)\n- 'multi_choice' — multiple text options\n- 'confirm' — yes/no (use for deletions, destructive actions)\n- 'number' — numeric input\n\n## Rules\n\n- Do NOT use for questions you can answer from context.\n- Ask during an ongoing task when new ambiguity or a blocker requires user input.\n- Prefer visual_choice over text choice for anything design-adjacent.\n- Prefer attempting + showing results over asking upfront on non-design tasks.",
  parameters: {"properties":{"choices":{"description":"Available choices (required for 'choice' and 'multi_choice' types)","items":{"type":"string"},"type":"array"},"default":{"description":"Default value if user doesn't respond or for text input placeholder","type":"string"},"description":{"description":"Optional detailed description providing context about why you're asking","type":"string"},"options":{"description":"Structured options for a singleton choice question. Each option has a unique value, a non-empty label, and an optional description.","items":{"properties":{"description":{"type":"string"},"label":{"type":"string"},"value":{"type":"string"}},"type":"object"},"type":"array"},"priority":{"default":"medium","description":"Priority level for the question","enum":["low","medium","high","critical"],"type":"string"},"question":{"description":"A single question to ask. Exactly one of question or questions must be supplied.","type":"string"},"questions":{"description":"A questionnaire of related questions. Exactly one of question or questions must be supplied.","items":{"properties":{"allow_other":{"description":"Whether the user may provide a custom answer. Defaults to true when omitted.","type":"boolean"},"id":{"description":"Unique, non-empty identifier for this question.","type":"string"},"label":{"description":"Optional short, non-empty heading for the question.","type":"string"},"options":{"description":"Available structured options, each with a unique value and non-empty label.","items":{"properties":{"description":{"type":"string"},"label":{"type":"string"},"value":{"type":"string"}},"type":"object"},"type":"array"},"prompt":{"description":"The question shown to the user.","type":"string"}},"type":"object"},"type":"array"},"timeout":{"default":"300","description":"Timeout in seconds (default: 300, 0 for no timeout)","type":"integer"},"title":{"description":"Optional title for the question dialog","type":"string"},"type":{"default":"text","description":"Type of question: 'text' for free-form input, 'choice' for single selection, 'multi_choice' for multiple selections, 'confirm' for yes/no, 'number' for numeric input, 'visual_choice' for a visual primitive picker","enum":["text","choice","multi_choice","confirm","number","visual_choice"],"type":"string"},"visual":{"additionalProperties":{},"description":"Visual primitive payload (required when type == 'visual_choice'). Shape: {kind: 'options'|'cards'|'split'|'markdown'|'mermaid'|'raw_html', ...primitive fields}","properties":{},"type":"object"}},"type":"object"},
};

export const ENTER_PLAN_MODE_CONTRACT: ToolContract = {
  description: "Switch to plan mode to design an approach before implementing.\n\nUse this tool when you receive a complex task that requires careful upfront design. In plan mode you should:\n1. Explore the codebase to understand the relevant context\n2. Resolve the decision tree with the user — ask one question at a time with your recommended answer\n3. Write your plan to a text or Markdown file inside the active workspace\n4. Call exit_plan_mode when ready for user approval\n\nIMPORTANT: In plan mode, you must resolve requirements with the user BEFORE writing the plan.\nWalk the decision tree branch-by-branch. For each unresolved decision:\n1. Ask one question at a time via ask_user_question\n2. Include your recommended answer (based on codebase exploration)\n3. Wait for user response before moving to the next decision\n4. If the answer can be found in the codebase, explore it yourself instead of asking\n\nPlan mode is an approval ceremony, not a permission boundary. Normal tool permissions,\nworkspace limits, and safety controls continue to apply.",
  parameters: {"additionalProperties":false,"properties":{},"required":[],"type":"object"},
};

export const EXIT_PLAN_MODE_CONTRACT: ToolContract = {
  description: "Present your plan for user approval and exit plan mode.\n\nRECOMMENDED: Write your plan to a text or Markdown file inside the active workspace,\nthen call exit_plan_mode with plan_file set to that file's path. The file contents\nare copied into conversation storage before approval.\n\nAlternatively, pass the full plan content directly in the plan parameter.\n\nThe user will review and can:\n- Approve (y): you begin implementing\n- Approve + compact context (c): context cleared, then implement\n- Reject with feedback (n): revise your plan and call exit_plan_mode again\n\nThis tool BLOCKS until the user responds - do not set a timeout.\nOnly call after enter_plan_mode.",
  parameters: {"additionalProperties":false,"properties":{"plan":{"description":"The complete implementation plan in Markdown format. Use either plan or plan_file, not both.","type":"string"},"plan_file":{"description":"Path to a UTF-8 text or Markdown plan inside the active workspace. Relative paths resolve from the workspace root. Use either plan_file or plan, not both.","type":"string"}},"required":[],"type":"object"},
};

export const CONTRACTS: Record<string, ToolContract> = {
  ask_user_question: ASK_USER_QUESTION_CONTRACT,
  enter_plan_mode: ENTER_PLAN_MODE_CONTRACT,
  exit_plan_mode: EXIT_PLAN_MODE_CONTRACT,
};

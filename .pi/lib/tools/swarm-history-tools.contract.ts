/** Model-facing contracts for the tools registered by this module. */
import type { ToolContract } from "../runtime/tool-contract.ts";

export const HISTORYSEARCH_CONTRACT: ToolContract = {
  description: "Search prior conversations by title, preview, or substantive message text. Defaults to the current workspace (the common case — just pass a query). Set scope=\"all\" to search across every workspace, but only do that when the current workspace has no match or the user explicitly asks to look across projects. You normally do NOT need to pass workspace_path; it is an advanced override for pinning one exact absolute workspace. Returns bounded conversation metadata and provenance, not full transcripts. Use HistoryGet only for selected conversation IDs.",
  parameters: {"properties":{"case_sensitive":{"default":false,"description":"Match case-exactly. Applies to both plain query terms and `regex`. Default false (case-insensitive; the regex is compiled with a leading (?i) so character classes still behave correctly). Set true to distinguish `Error` from `error`, or a Go exported identifier from a local one.","type":"boolean"},"exclude_runtime":{"default":false,"description":"Ignore runtime-injected pseudo-messages when matching message bodies. A `user` role is NOT proof a human typed it: the runtime injects `<system-reminder`, `[SCHEDULED]`, `## MCP Context`, `## Current Tasks` and `**Current Mode**` blocks into the user turn. Set true when you are looking for something a PERSON actually said and do not want scaffolding boilerplate to match. Same predicate as HistoryGet's human_only.","type":"boolean"},"fields":{"description":"Restrict matching to these fields. Default (omitted) searches all of them, which is today's behavior. Use [\"title\"] to find conversations ABOUT a topic rather than ones that merely mention it in passing; use [\"body\"] to find a string that was said mid-conversation. Note `body` is only consulted when search_body is on.","items":{"enum":["title","preview","body"],"type":"string"},"type":"array"},"limit":{"default":10,"maximum":50,"minimum":1,"type":"integer"},"max_snippets":{"default":3,"description":"Maximum snippets returned per result. Only meaningful with snippet=true.","maximum":10,"minimum":1,"type":"integer"},"min_messages":{"description":"Exclude conversations with fewer stored messages.","minimum":0,"type":"integer"},"ngram":{"default":1,"description":"Phrase length for stats, from 1 to 5. Use 1 for vocabulary; 2 or 3 for useful recurring phrases. Higher values usually produce near-unique strings. Only meaningful with stats=true.","maximum":5,"minimum":1,"type":"integer"},"order":{"default":"desc","enum":["asc","desc"],"type":"string"},"origin":{"description":"Filter by conversation origin.","enum":["interactive","subagent","headless"],"type":"string"},"query":{"description":"Case-insensitive title, preview, and message-body query. Empty lists recent conversations.","type":"string"},"regex":{"description":"Go RE2 regular expression applied to the selected fields. Reach for this when a plain query cannot express the shape you want: commit prefixes (`fix\\(hooks\\):\\s+\\w+`), identifiers (`PR #\\d{3}`), alternation (`panic|fatal error`), or anchors. RE2 syntax only — lookahead and backreferences are NOT supported and return a clear error. Combines with `query`: the query narrows candidates cheaply and the regex then filters them, so both must match. An invalid pattern is reported as an error, never silently ignored.","type":"string"},"scope":{"default":"current","description":"Default \"current\" searches this workspace. Use \"all\" only when the current workspace has no match or the user explicitly asks to search across all projects.","enum":["current","all"],"type":"string"},"search_body":{"description":"Search substantive user/assistant message text in addition to title and preview. Defaults to true when query or regex is non-empty.","type":"boolean"},"segment_kind":{"description":"Filter matches to one segment kind: message prose, tool_call input, or tool_result output.","enum":["message","tool_call","tool_result"],"type":"string"},"snippet":{"default":false,"description":"Return the matching text with surrounding context plus which field matched, instead of only a generic preview. Turn this on whenever you need to know WHY a conversation matched — it is the difference between a list of IDs and actual evidence. Snippets are bounded in length and count and never split a multi-byte character.","type":"boolean"},"snippet_context":{"default":60,"description":"Bytes of surrounding context on each side of a match in a snippet. Only meaningful with snippet=true.","maximum":200,"minimum":1,"type":"integer"},"sort":{"description":"Sort results by relevance, recency, or message count. Defaults to relevance for a query or regex and recency when both are empty.","enum":["relevance","recency","message_count"],"type":"string"},"stats":{"default":false,"description":"Return a frequency table INSTEAD OF a conversation list. This changes the shape of the output. Use it to study vocabulary or recurring phrasing across many conversations; do not use it when you are trying to find and open one conversation.","type":"boolean"},"tool_name":{"description":"Filter to segments for one tool name, for example \"Bash\". This matches only tool segments, so setting it searches tool traffic rather than ordinary conversation prose.","type":"string"},"tool_outcome":{"description":"Filter tool results by outcome: any, failed, or succeeded. Setting this implies tool-result segments because only tool results have an outcome.","enum":["any","failed","succeeded"],"type":"string"},"top_terms":{"default":50,"description":"Maximum frequency rows to return. Defaults to 50 and is capped at 500 because large frequency tables can flood a terminal or model context window. Only meaningful with stats=true.","maximum":500,"minimum":1,"type":"integer"},"workspace_path":{"description":"Advanced: exact absolute workspace path to search. You rarely need this — prefer the default current scope, or scope=all. Mutually exclusive with scope=all.","type":"string"}},"type":"object"},
};

export const HISTORYGET_CONTRACT: ToolContract = {
  description: "Read a bounded excerpt from one prior conversation selected with HistorySearch. Defaults to the current workspace — just pass conversation_id. If the conversation lives in another project (e.g. it came from a scope=all search) OR the user explicitly asks to read across projects, set all_workspaces=true to read it by ID regardless of directory. You do NOT need to pass workspace_path; that is an advanced exact-path override. Returns message IDs, roles, timestamps, and truncation provenance.",
  parameters: {"properties":{"all_workspaces":{"default":false,"description":"Read the conversation by ID regardless of which workspace it belongs to. Use this when the ID came from a scope=all search or the user asks to read across projects. Preferred over passing workspace_path.","type":"boolean"},"conversation_id":{"type":"string"},"human_only":{"default":false,"description":"Omit system/tool messages and machine-only reminder boilerplate.","type":"boolean"},"max_chars":{"default":12000,"maximum":50000,"minimum":1,"type":"integer"},"max_messages":{"default":20,"maximum":100,"minimum":1,"type":"integer"},"offset":{"description":"Zero-based start position in the original stored message sequence. Uses max_messages as the page size. Mutually exclusive with tail.","minimum":0,"type":"integer"},"tail":{"description":"Return the final N stored messages. Mutually exclusive with offset.","maximum":100,"minimum":1,"type":"integer"},"workspace_path":{"description":"Advanced: exact absolute workspace path containing the conversation. You normally do not need this — use the current-workspace default or all_workspaces=true. Ignored when all_workspaces=true.","type":"string"}},"required":["conversation_id"],"type":"object"},
};

export const CONTRACTS: Record<string, ToolContract> = {
  HistorySearch: HISTORYSEARCH_CONTRACT,
  HistoryGet: HISTORYGET_CONTRACT,
};

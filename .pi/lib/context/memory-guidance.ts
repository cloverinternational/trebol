/** Shared semantic guidance, not an authorization or automatic classification gate. */
export const MEMORY_KNOWLEDGE_GUIDANCE = "Memory records what is known: project architecture, domain facts, terminology, decisions and rationale, constraints, preferences, and relevant ongoing context. A project's established process is knowledge about that project. Reusable instructions for how an agent should investigate, implement, debug, or verify belong in skills, not memory. Separate factual context from procedural advice when an observation contains both. Preserve evidence, uncertainty, and freshness; later corrections may invalidate earlier claims. Do not treat an agent's success claim as verified fact.";

export const MEMORY_SCOPE_GUIDANCE = "Use repository scope for shared project knowledge; worktree scope for unmerged or experimental facts; session scope for local conversational context. Use global only for explicitly shareable cross-project facts or preferences, not merely because a lesson sounds reusable. Do not duplicate a fact into both repository and global scope by default. Reading combined scopes does not authorize wider writes.";

export const MEMORY_REVIEW_GUIDANCE = `${MEMORY_KNOWLEDGE_GUIDANCE} ${MEMORY_SCOPE_GUIDANCE} After discovering useful knowledge, search existing memory before saving or correcting it through the owning memory tool. Include evidence references and distinguish observations from assumptions. Do not store secrets or raw transcripts, turn unverified plans into facts, or copy routine progress logs. No useful new knowledge is a valid outcome. Improve relevant skills separately when reusable procedures were learned.`;

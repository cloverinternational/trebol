# Reloaded memory retrieval recheck

## Live calls and inspected source

1. `context_search(query="CapabilityManifestState.apply")` returned `candidate-leads`. It included the correct worktree TaskManage Q&A `48b544e64653fb3827c566999a17400e`, revision `d54c2d6ecb5fb594fc4d263b74272fcd`, with the answer and `.pi/lib/runtime/swarm-transport-parity.ts#L283-L335`; it also included a concise candidate fact and three self-referential task-review records. Direct `memory_history(get, scope=worktree, id=...)` returned that same candidate/revision. The cited implementation has `buildCapabilityManifest` and `CapabilityManifestState.apply` in those lines.
2. `context_search(query="Where does a hypothetical non-existent quasar spool 934582 live?")` returned `no-result` with empty evidence and an explicit note that the index contains no section about it. This is an improvement over the earlier false-positive candidate leads.
3. `memory_history(recall, scope=worktree, status=candidate, query="CapabilityManifestState.apply", limit=5)` returned `ok` with the correct TaskManage Q&A, **but also three meta-records about our own retrieval evaluation**. Its note explicitly said no selected card documented the current API or completed this re-test. The result thus carries useful evidence yet remains noisy and potentially misleading.
4. The identical recall with `limit=1` returned **four** records. This is an observed contract bug: `.pi/extensions/40-state/memory-history.ts#L179` accepts 1–12, but `.pi/lib/context/memory-agent.ts#L49` always requests `MAX_CARDS` and ignores `input.limit` when returning selected records.
5. `memory_history(recall, scope=worktree, status=candidate, query=quasar-question)` returned `no-result` with no knowledge. A unique nonsense key likewise returned `no-result`. `recall(scope=repository, status=verified, query="PageIndex")` returned `no-result` with a note: the lone verified architecture decision did not answer this *current re-test task*. That is an honest relevance judgment, but a caller seeking the historical PageIndex decision by topic would see an empty result despite the record existing.
6. `recall(scope=worktree, status=candidate, limit=13)` was rejected (`recall limit must be 1–12`); `scope=session` was rejected. Direct inspection after the calls found the target still a worktree **candidate** with revision `d54c2d6ecb5fb594fc4d263b74272fcd`, and the repository PageIndex decision still **verified** at its original revision. These checks do not establish a universal no-write proof; they establish the inspected records did not change.

## Effectiveness

The reloaded tools now genuinely find the intended candidate and honestly decline the non-existent topic. They are **not yet consistently selective**: evaluation-related records repeat the queried identifier and crowd the answer; focused-task wording can suppress a directly requested verified topic. `limit` is visibly ignored by the final recall result. No live model failure/timeout or child tool-call trace was exercised here; `no-result` above means no answer, not an observed failure.

Next: honor `limit` at both candidate shortlist and returned selection; prevent self-referential task-progress records from outranking substantive answers; distinguish task-guided recall from exact topical lookup so a verified decision remains discoverable when explicitly requested.

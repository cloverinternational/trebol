# Reloaded mechanism recall: live agent-facing check

## Call/record comparison

1. `memory_history(recall, scope=worktree, status=candidate, mode=task, limit=2, query="memory_history recall limits")` returned `ok`. It selected TaskManage Q&A `4eebcab45b6b147d0af91299a1d6bdbc`, with a **readable source excerpt** of `.pi/extensions/40-state/memory-history.ts#L174-L188`. The excerpt actually contains scope selection, rejection of `session`, validation of `limit` 1–12, focused task lookup, model choice, and `selectTaskMemory` invocation. It also selected `b49d4dfb95dccf51a1c332f82c57835f`, whose readable implementation excerpts cover `.pi/lib/context/knowledge-pageindex.ts#L101-L118` and the meta-filter definition. The response labels both worktree `candidate`, with record IDs, revisions and citations. This proves the reloaded path gives the agent file contents instead of bare file references; it does **not** prove the TaskManage summaries are verified facts.
2. `memory_history(recall, mode=task, query="memory_history recall limits evaluation")` returned `ok` with evaluation Q&A (including `4eeb...` and `f927e0577e2854e17fe1acf392655b37`), both with readable cited source excerpts. This countercase confirms that a request about evaluations is not indiscriminately hidden.
3. `memory_history(recall, mode=task, query="nonsense-zqv-no-such-memory-934582")` returned `no-result`, empty knowledge. The same query with `status=verified` also returned `no-result`; these are honest absences, not observed tool/model errors.
4. Before/after direct store inspection found `4eeb...` and `f927...` still worktree candidates at revisions `1f2fe87933ff245389a7950b134077c4` and `6093694d9a2f13f47e650b7fa83f9205`. No write or promotion was invoked. The selected source text was checked against the current implementation, not accepted from the citation alone.

## Effectiveness and limits

**Improved, but incomplete:** the memory agent can inspect and return primary code excerpts with candidate Q&A, and the read-only tool's limit of two held in the first call. The second card in that call covered the PageIndex shortlist/meta filter, not the requested limit directly; the agent still has some adjacent-evidence noise. The model may return `invalid-selection` on another invocation (one earlier pre-reload call did), and no child tool-call transcript is retained in the parent output. This check did not induce a live timeout, malformed source reference, unavailable model, or cross-workspace scope attempt. Do not extrapolate from these calls to those untested paths.
